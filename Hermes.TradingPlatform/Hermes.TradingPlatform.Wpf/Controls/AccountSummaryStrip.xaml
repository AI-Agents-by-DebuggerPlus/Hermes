<UserControl x:Class="Hermes.TradingPlatform.Wpf.Controls.AccountSummaryStrip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource Brush.BgPanel}"
            BorderBrush="{StaticResource Brush.Border}"
            BorderThickness="0,0,0,1"
            Padding="16,10">
        <StackPanel>
        <UniformGrid Columns="6">
            <StackPanel Margin="0,0,12,0">
                <TextBlock Text="Balance" Foreground="{StaticResource Brush.TextMuted}" FontSize="10" />
                <TextBlock Text="{Binding Account.Balance, StringFormat=N2}" FontSize="16" FontWeight="SemiBold" />
                <TextBlock Text="USDT" Foreground="{StaticResource Brush.TextMuted}" FontSize="10" />
            </StackPanel>
            <StackPanel Margin="0,0,12,0">
                <TextBlock Text="Equity" Foreground="{StaticResource Brush.TextMuted}" FontSize="10" />
                <TextBlock Text="{Binding Account.Equity, StringFormat=N2}" FontSize="16" FontWeight="SemiBold" />
            </StackPanel>
            <StackPanel Margin="0,0,12,0">
                <TextBlock Text="Free margin" Foreground="{StaticResource Brush.TextMuted}" FontSize="10" />
                <TextBlock Text="{Binding Account.FreeMargin, StringFormat=N2}" FontSize="16" FontWeight="SemiBold" />
            </StackPanel>
            <StackPanel Margin="0,0,12,0">
                <TextBlock Text="Used margin" Foreground="{StaticResource Brush.TextMuted}" FontSize="10" />
                <TextBlock Text="{Binding Account.UsedMargin, StringFormat=N2}" FontSize="16" FontWeight="SemiBold" />
            </StackPanel>
            <StackPanel Margin="0,0,12,0">
                <TextBlock Text="PnL today" Foreground="{StaticResource Brush.TextMuted}" FontSize="10" />
                <TextBlock Text="{Binding Pnl.Today, StringFormat=N2}" FontSize="16" FontWeight="SemiBold"
                           Foreground="{StaticResource Brush.Positive}" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Open positions" Foreground="{StaticResource Brush.TextMuted}" FontSize="10" />
                <TextBlock Text="{Binding OpenPositionsCount}" FontSize="16" FontWeight="SemiBold" />
            </StackPanel>
        </UniformGrid>
        <TextBlock Text="{Binding TradeStatusLine}"
                   Foreground="{StaticResource Brush.TextMuted}"
                   FontSize="11"
                   TextWrapping="Wrap"
                   Margin="0,8,0,0" />
        </StackPanel>
    </Border>
</UserControl>
