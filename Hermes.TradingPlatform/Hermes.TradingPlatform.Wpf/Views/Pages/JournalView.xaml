<UserControl x:Class="Hermes.TradingPlatform.Wpf.Views.Pages.JournalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="Торговый журнал · исполнения, realized PnL, баланс до/после"
                   Foreground="{StaticResource Brush.TextMuted}"
                   Margin="0,0,0,12" />
        <Border Style="{StaticResource PanelCardStyle}">
            <DataGrid ItemsSource="{Binding Entries}" Style="{StaticResource DataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=HH:mm:ss}" Width="80" />
                    <DataGridTextColumn Header="Kind" Binding="{Binding Kind}" Width="64" />
                    <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="90" />
                    <DataGridTextColumn Header="Side" Binding="{Binding Side}" Width="52" />
                    <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="70" />
                    <DataGridTextColumn Header="Price" Binding="{Binding FillPrice, StringFormat=N2}" Width="80" />
                    <DataGridTextColumn Header="Realized" Binding="{Binding RealizedPnl, StringFormat=N2}" Width="80" />
                    <DataGridTextColumn Header="Fee" Binding="{Binding Fee, StringFormat=N4}" Width="64" />
                    <DataGridTextColumn Header="Balance −" Binding="{Binding BalanceBefore, StringFormat=N2}" Width="96" />
                    <DataGridTextColumn Header="Balance +" Binding="{Binding BalanceAfter, StringFormat=N2}" Width="96" />
                    <DataGridTextColumn Header="Order" Binding="{Binding OrderId}" Width="80" />
                    <DataGridCheckBoxColumn Header="RO" Binding="{Binding ReduceOnly}" Width="40" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </StackPanel>
</UserControl>
