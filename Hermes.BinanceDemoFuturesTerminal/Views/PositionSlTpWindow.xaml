<Window x:Class="Hermes.BinanceDemoFuturesTerminal.Views.PositionSlTpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SL / TP позиции"
        Height="240" Width="420"
        Background="#0C0E12"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Foreground="#EAECEF" FontSize="14" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" Margin="0,6,0,12" Foreground="#848E9C" FontSize="10" TextWrapping="Wrap"
                   Text="Оставьте поле пустым, чтобы снять соответствующий ордер."/>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Stop-Loss" Foreground="#848E9C" VerticalAlignment="Center"/>
            <TextBox x:Name="StopLossBox" Grid.Column="1" Height="26" Background="#20242B" Foreground="#EAECEF"
                     BorderBrush="#2B3139" Padding="6,4"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Take-Profit" Foreground="#848E9C" VerticalAlignment="Center"/>
            <TextBox x:Name="TakeProfitBox" Grid.Column="1" Height="26" Background="#20242B" Foreground="#EAECEF"
                     BorderBrush="#2B3139" Padding="6,4"/>
        </Grid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Padding="12,6" Margin="0,0,8,0" Click="OnCancel"/>
            <Button Content="Применить" Padding="16,6" FontWeight="Bold" Click="OnApply"/>
        </StackPanel>
    </Grid>
</Window>
