<UserControl x:Class="Hermes.BinanceDemoFuturesTerminal.Controls.TradeStatsSummaryPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:Hermes.BinanceDemoFuturesTerminal.Models">
    <Border Background="#181A20" BorderBrush="#2B3139" BorderThickness="1" CornerRadius="4" Padding="10,8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,8">
                <TextBlock Text="ИТОГИ" FontSize="11" FontWeight="Bold" Foreground="#EAECEF" VerticalAlignment="Center"/>
                <TextBlock HorizontalAlignment="Right" FontSize="9" Foreground="#848E9C" VerticalAlignment="Center"
                           Text="{Binding TradeStatsStatusText, Mode=OneWay}"/>
            </Grid>

            <Grid Grid.Row="1" Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="72"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="1" Text="PnL" FontSize="9" Foreground="#848E9C" HorizontalAlignment="Right" Margin="0,0,8,0"/>
                <TextBlock Grid.Column="2" Text="Комиссия" FontSize="9" Foreground="#848E9C" HorizontalAlignment="Right"/>
            </Grid>

            <ItemsControl Grid.Row="2" ItemsSource="{Binding TradeStatsRows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:TradeStatsPeriodRow}">
                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="72"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding PeriodLabel}" FontSize="10" Foreground="#848E9C" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding PnlDisplay}" FontSize="10" FontWeight="SemiBold"
                                       HorizontalAlignment="Right" Margin="0,0,8,0" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#848E9C"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsPnlPositive}" Value="True">
                                                <Setter Property="Foreground" Value="#0ECB81"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsPnlNegative}" Value="True">
                                                <Setter Property="Foreground" Value="#F6465D"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Grid.Column="2" Text="{Binding CommissionDisplay}" FontSize="10"
                                       Foreground="#848E9C" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
