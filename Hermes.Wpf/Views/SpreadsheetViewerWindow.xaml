<Window x:Class="Hermes.Wpf.Views.SpreadsheetViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Просмотр таблицы"
        Height="640"
        Width="980"
        MinHeight="480"
        MinWidth="760"
        Background="{StaticResource AppBackgroundBrush}"
        Foreground="{StaticResource PrimaryTextBrush}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="*" />
            <RowDefinition Height="8" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0">
            <Button Width="110"
                    Margin="0,0,8,8"
                    Content="Открыть…"
                    Click="OpenFileButton_OnClick" />
            <TextBlock Margin="0,6,8,8"
                       VerticalAlignment="Center"
                       Text="Лист:" />
            <ComboBox x:Name="SheetComboBox"
                      Width="160"
                      Margin="0,0,12,8"
                      SelectionChanged="SheetComboBox_OnSelectionChanged" />
            <TextBlock Margin="0,6,8,8"
                       VerticalAlignment="Center"
                       Text="С:" />
            <DatePicker x:Name="FilterFromPicker"
                        Width="130"
                        Margin="0,0,8,8" />
            <TextBlock Margin="0,6,8,8"
                       VerticalAlignment="Center"
                       Text="По:" />
            <DatePicker x:Name="FilterToPicker"
                        Width="130"
                        Margin="0,0,8,8" />
            <Button Width="100"
                    Margin="0,0,8,8"
                    Content="Фильтр"
                    Click="ApplyFilterButton_OnClick" />
            <Button Width="100"
                    Margin="0,0,8,8"
                    Content="Сброс"
                    Click="ClearFilterButton_OnClick" />
        </WrapPanel>

        <TextBlock x:Name="FilePathText"
                   Grid.Row="2"
                   TextWrapping="Wrap"
                   Foreground="{StaticResource MutedTextBrush}"
                   FontSize="12"
                   Text="Файл не выбран" />

        <DataGrid x:Name="SheetGrid"
                  Grid.Row="4"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True" />

        <TextBlock x:Name="SummaryText"
                   Grid.Row="6"
                   FontSize="12"
                   Foreground="{StaticResource PrimaryTextBrush}"
                   Text="" />
    </Grid>
</Window>
