<Window x:Class="Hermes.EnglishLearning.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Лог"
        Width="720"
        Height="420"
        WindowStartupLocation="CenterOwner"
        Background="#0B0E11"
        Foreground="#EAECEF"
        Closing="Window_OnClosing">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="PathText" Foreground="#AAB2C0" Margin="0,0,0,6" TextWrapping="Wrap" />
        <TextBox x:Name="LogBox"
                 Grid.Row="1"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 Background="#161B22"
                 Foreground="#EAECEF"
                 BorderBrush="#30343D"
                 FontFamily="Consolas"
                 FontSize="12" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Очистить окно" Padding="10,4" Margin="0,0,8,0" Click="Clear_OnClick" />
            <Button Content="Закрыть" Padding="10,4" IsCancel="True" Click="Close_OnClick" />
        </StackPanel>
    </Grid>
</Window>
