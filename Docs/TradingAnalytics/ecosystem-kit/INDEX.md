# Trading ecosystem — INDEX (читай только это сначала)

**Правило контекста:** не загружай все файлы экосистемы в память и не копируй их в `~/.hermes/MEMORY.md`.  
Открой **один** файл ниже — только если он нужен для *текущего* вопроса пользователя.

| Когда нужно | Файл |
|-------------|------|
| Карта приложений / проектов | `apps.md` |
| Живые данные Futures/HWT (snapshot, bridge) | `live-data.md` |
| Скриншот графика MT5 / HWT | `howto-chart-screenshot.md` |
| Рыночный контекст без ордера (цена, позиции) | `howto-market-context.md` |
| **Плотности / стенки стакана** | `howto-density.md` ← screener `%LocalAppData%\HermesDensity\` |

Если вопрос про **плотности / liquidity walls / отскок от стенки** — сразу `howto-density.md` (и snapshot), не тащи весь TradingModeReport.

## QA / тесты

| Когда нужно | Файл / инструмент |
|-------------|-------------------|
| Автотесты экосистемы | `qa/run_all_checks.ps1` |
| Запуск приложений для глаз | **Hermes.Wpf Launcher** → Testing |
| Что смотреть глазами | `qa/MANUAL_CHECKLIST.md` |
| Как агенту гонять автотесты | `qa/README.md` |


## Этот проект

**Trading Analytics** — навигационный хаб: ответы и действия по рынку через уже существующие терминалы и IPC, а не дублирование их кода здесь.

## Запрещено по умолчанию

- Не открывать сделки «на всякий случай».
- Не тащить полный `TradingModeReport` в ответ, пока пользователь не просит разбор архитектуры.
- Project-specific заметки и задачи — только в `hermes/project.md`.
