<Window x:Class="Hermes.RemoteTerminal.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Лог — Remote Terminal"
        Width="720" Height="440"
        Background="{StaticResource Bg}"
        Foreground="{StaticResource Text}"
        Closing="Window_OnClosing">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PathText" Foreground="{StaticResource Muted}" Margin="0,0,0,6" TextWrapping="Wrap"/>
        <TextBox x:Name="LogBox"
                 Grid.Row="1"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"
                 Background="{StaticResource InputBg}"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Отправить в Supabase" Click="SendSupabase_OnClick"/>
            <Button Content="Очистить окно" Click="Clear_OnClick"/>
            <Button Content="Закрыть" IsCancel="True" Click="Close_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
