namespace Hermes.DesktopCapture.Models;

public enum ScreenRegionRole
{
    ApplicationWindow,
    TitleBar,
    MenuBar,
    Editor,
    MinimizeButton,
    MaximizeButton,
    CloseButton,
    TaskBar,
}
